export default function Loading() {
  return (
    <div className="page-loading" role="status" aria-live="polite" aria-label="Carregando">
      <div className="page-loading-bar" />
    </div>
  );
}
